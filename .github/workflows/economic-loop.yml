name: economic-loop

on:
  push:
    branches: [master]
  pull_request:

permissions:
  contents: read

jobs:
  economic-loop:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Shell syntax
        run: |
          bash -n loop/economic_loop.sh
          bash -n demo/fake_codex.sh
          bash -n demo/test_economic_loop.sh
      - name: Deterministic accounting test
        run: |
          chmod +x demo/fake_codex.sh
          bash demo/test_economic_loop.sh
